# Factory Function

A Factory Function creates and returns an object instead of performing the work immediately.

Example:

redditSearchAgent()

↓

Returns Runnable

↓

Caller executes Runnable using invoke() or stream()

---

The agent function builds the pipeline.

It does not execute it.